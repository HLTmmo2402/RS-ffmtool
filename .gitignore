# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — KHÔNG commit khoá Supabase
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# === QUAN TRỌNG: KHÔNG commit file Excel gốc (chứa mật khẩu/credentials plaintext) ===
*.xlsx
*.xls
# Dữ liệu gốc nhạy cảm (Excel credentials) di dời vào đây
/_private/

# Seed dữ liệu THẬT (chứa tên/địa chỉ/SĐT khách) — KHÔNG commit. seed_templates.sql thì OK.
/supabase/seed_data.sql

# File export đơn từ Cotik/sàn (chứa PII khách hàng) — KHÔNG commit
/orders_*.csv
